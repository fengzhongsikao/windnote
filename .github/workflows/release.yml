name: Release

on:
  push:
    paths:
      - 'wails.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: darwin/universal
            artifact_name: windnote-darwin-universal
            upload_path: build/bin/windnote-darwin-universal.zip
            build_tags: ''

          - os: windows-latest
            platform: windows/amd64
            artifact_name: windnote-windows-amd64
            upload_path: build/bin/windnote-windows-amd64.zip
            build_tags: ''

          - os: ubuntu-latest
            platform: linux/amd64
            artifact_name: windnote-linux-amd64
            upload_path: build/bin/windnote-linux-amd64.tar.gz
            build_tags: webkit2_41

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Install system dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            pkg-config \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Inject version from wails.json
        shell: bash
        run: |
          VERSION=$(node -e "console.log(require('./wails.json').info.productVersion)")
          echo "Release version: $VERSION"
          sed -i '' "s/return \"[0-9.]*\"/return \"$VERSION\"/" zhanbu/app.go || \
          sed -i "s/return \"[0-9.]*\"/return \"$VERSION\"/" zhanbu/app.go

      - name: Build
        run: wails build -platform ${{ matrix.platform }} -tags ${{ matrix.build_tags }} -clean

      - name: Package (macOS)
        if: matrix.os == 'macos-latest'
        run: cd build/bin && zip -ry windnote-darwin-universal.zip windnote.app

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cd build/bin
          Compress-Archive -Path * -DestinationPath windnote-windows-amd64.zip

      - name: Package (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: cd build/bin && tar czf windnote-linux-amd64.tar.gz windnote

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.upload_path }}
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Determine version from wails.json
        id: version
        run: |
          VERSION=$(node -e "console.log(require('./wails.json').info.productVersion)")
          echo "tag=v${VERSION}" >> $GITHUB_OUTPUT
          echo "name=${VERSION}" >> $GITHUB_OUTPUT
          echo "Version: ${VERSION}"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: find artifacts -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.name }}
          files: artifacts/**/*
          generate_release_notes: true
          fail_on_unmatched_files: true
